# Build production Vite : base de l’API (sans slash final).
# Remplacer par l’URL publique du service API Railway (Networking) ; tester : GET <URL>/api/health (200 OK)
VITE_API_URL=https://elyanis-production.up.railway.app
