# Root `src/` Inventory (Archived)

This document was created during a transitional period when the repository contained **two** frontend trees.

As of 2026-04-16, the repository has been consolidated to a **single** frontend at the repository root (`src/`).

If you need a fresh unused-file audit, regenerate it from the current tree rather than relying on historical notes here.
